# placeholder for running dbt deps on image build. real profiles.yml file should be mounted on docker run
dbt_server:
  target: postgres_test
  outputs:
    postgres_test:
      type: postgres
      host: hostname
      user: username
      password: password
      port: 5432
      dbname: database
      schema: public
